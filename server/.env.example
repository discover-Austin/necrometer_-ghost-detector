# Necrometer Proxy Server Environment Configuration
# Copy this file to .env and fill in your actual values

# REQUIRED: Your Google Gemini API key
API_KEY=your_gemini_api_key_here

# REQUIRED: Secret for signing JWT tokens (use a strong random string)
JWT_ISSUER_SECRET=replace-this-with-strong-random-secret

# REQUIRED: Token that clients must provide to get a JWT (use a strong random string)
SHARED_ISSUANCE_TOKEN=replace-me-with-issuance-secret

# Optional: Port for the server (defaults to 4000)
PORT=4000
