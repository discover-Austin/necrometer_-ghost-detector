# Example environment variables for server
# Must be set in production or when running the server for real LLM calls
API_KEY=your-google-genai-api-key-here
JWT_ISSUER_SECRET=replace-with-a-strong-random-secret
SHARED_ISSUANCE_TOKEN=replace-with-a-shared-issuance-token
PORT=4000
