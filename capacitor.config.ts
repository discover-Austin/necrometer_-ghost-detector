import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ghosted_necrometer.app',
  appName: 'Ghosted-Necrometer',
  webDir: 'dist'
};

export default config;
