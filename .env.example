# Necrometer Environment Configuration
# Copy this file to .env.local and fill in your actual values

# Optional: Direct Gemini API key for client-side usage (NOT recommended for production)
# If you want to use the proxy server (recommended), leave this empty
API_KEY=

# Proxy Server Configuration (Recommended for production)
# The proxy server URL where your backend is hosted
PROXY_BASE_URL=

# Shared issuance token for requesting JWT from proxy server
SHARED_ISSUANCE_TOKEN=
